using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Simple script que j'attache à un gameobject pour retrouver celui-ci plus facilement
//avec gameObject.GetComponent<ShootingPoint>(). 
//honnêtement pas la meilleure méthode j'en suis quasiment sûr, mais ça fonctionne

//(attaché aux prefab de l'ennemi, dans le gameobject shooting position)
public class ShootingPoint : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
